#include <iostream>
using namespace std;

int main() {
    int t;
    cin >> t;

    while (t--) {
        int x, y;
        cin >> x >> y;

        int distance = abs(y - x);
        int time = distance / 2 + (distance % 2);

        cout << time << endl;
    }

    return 0;
}
